#!/bin/bash

remote="origin"

if [ "$#" -ge 0 ]; then
    remote=$1
fi

git remote -v | grep "$remote" | head -n 1 | rev| sed 's![/:\t]! !g' | sed 's!  ! !g' | cut -d" " --fields=2,3 | rev | sed 's! !/!g'
